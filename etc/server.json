/*
  Server configurations.
 */
{
  "bind_port": "random",
  "session_handler": "sessionChecker",
  "bunyan": {
    "name": "base-server",
    "streams": [
      {
        "level": "error",
        "path": "./log/base-server-error.log"
      }
    ]
  },
  "static": {
    "default": "index.html",
    "directory": "./static"
  },
  "auth": {
    "test": "test"
  }
}
